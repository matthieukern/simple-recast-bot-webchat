// @flow
import React from "react";
import styled from "styled-components";

/** A container that wraps a message in the center of the chat. */
const NoMessageInfo = styled.div`
	width: 100%;
	height: 100%;
	display: flex;
	justify-content: center;
	align-items: center;
	font-size: 18pt;
	color: #c8c8c8;
`;

export default NoMessageInfo;
